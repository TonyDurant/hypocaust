= simple_form_for @blog, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :description
  = f.button :submit, :class => 'btn-primary'
  = link_to t('.cancel', :default => t("helpers.links.cancel")), blogs_path, :class => 'btn btn-default'
