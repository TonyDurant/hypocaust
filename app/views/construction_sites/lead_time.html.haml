- unless @lead_time_data.nil?
  .text-muted= t("metrics.updates_current")
  %hr/
  = timeline @lead_time_data, height: "#{@lead_time_data.count <= 3 ? @lead_time_data.count * 70 + 30 : @lead_time_data.count * 55 + 15}px"
